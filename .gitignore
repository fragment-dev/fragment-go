.DS_Store
.idea
*.log
tmp/

fragment-go
.go-version

# Scratch dirs from the tests that compile generated code; normally cleaned up by
# the tests themselves, but left behind if they are interrupted.
internal/generated/keyedcheck-*
internal/generated/compilecheck-*
